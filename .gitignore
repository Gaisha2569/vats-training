# ===== ไฟล์ต้นฉบับ (source) — ไม่ต้องอัปขึ้นเว็บสาธารณะ =====
# เก็บสำรองไว้ในเครื่อง/ไดรฟ์เอง เพราะ git จะไม่ติดตามไฟล์เหล่านี้

# เสียงบันทึกดิบ + ดนตรีประกอบ
sound/
track/

# ภาพต้นฉบับทั้งหมด
pic/
picforclip/
picปอด/
xxx/
Screen/

# โฟลเดอร์งานเก่า / สำรอง
aa/
not use/
*.rar
*.zip

# เอกสารสคริปต์และไฟล์ชั่วคราวของ Word
*.docx
*.pdf
~$*
~WRL*.tmp

# ไฟล์ระบบ
.DS_Store
Thumbs.db
desktop.ini
